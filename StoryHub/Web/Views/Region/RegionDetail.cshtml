@using Web.Data.Stories
@using Web.Enumerations
@using Web.Miscellaneous
@using Web.Models

@model RegionDetailViewModel

@{
    ViewData["Title"] = "Region Details";
}

@using (Html.BeginForm("SaveRegionDetail", "Region", FormMethod.Post))
{
    <h1>
        @ViewData["Title"]
    </h1>

    <hr />

    @if (Model != null)
    {
        <div class="row">
            <label class="col-xl-2 label-text">
                Title
            </label>

            <div class="col-xl-4">
                @Html.TextBoxFor(
        Model => Model.Name,
        Model.Name,
        new {
        @class="form-control",
        }
        )
            </div>
        </div>

        <br />

        <div class="row">
            <label class="col-xl-2 label-text">
                Description
            </label>

            <div class="col-xl-4">
                @Html.TextBoxFor(
        Model => Model.Description,
        Model.Description,
        new {
        @class="form-control",
        }
        )
            </div>
        </div>

        <br />

        @Html.HiddenFor(
        Model => Model.RegionID,
        Model.RegionID
    )
    }

    <div class="row">
        <div class="col">
            <button type="submit" class="btn btn-primary">
                Save
            </button>

            @Html.ActionLink(
        "Back", "Index", "Region",
        new {},
        new {
        @class = "btn btn-secondary",
        })
        </div>
    </div>
}